# Netlify configuration for FAZO website (Next.js 14)
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "npm run build"
  # Use relative path only - never use absolute/Windows paths
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
